#include <vector>

std::vector <int> delRepeating(std::vector <int> array);

int binarySearch(std::vector<int> array, int x, int left = 0, int right = -1);

std::vector <int> Merge(std::vector <int> a, std::vector <int> b);